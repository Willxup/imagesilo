# ADR 0001：V1 使用单进程模块化单体

状态：已接受  
日期：2026-07-29

## 决定

V1 使用一个 Go 进程、一个 SQLite 数据库、一个本地数据目录、一个进程内交付索引和一个嵌入式 React 管理后台。

## 原因

图床的事实来源和交付路径位于同一主机。单进程可以用最短的一致性链路完成文件落盘、SQLite 提交和内存索引更新，不引入跨进程缓存失效与部署成本。

## 重新评估条件

只有单进程经过测量和优化后仍无法满足真实负载，并且拆分边界、独立伸缩收益和一致性成本都明确时，才重新评估。
